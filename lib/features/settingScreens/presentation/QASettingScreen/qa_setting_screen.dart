import 'package:flutter/material.dart';


class QASettingScreen extends StatelessWidget {
  const QASettingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Center(
          child: Text('QA'),
        ),
      ),
    );
  }
}
